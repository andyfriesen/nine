Executables produced by the ninec unit tests go here.

A lot of them depend on ClassLibrary1.dll, so don't remove that, or tests will drop like flies. (ClassLibrary1.dll is built from tests/Class1.cs)
